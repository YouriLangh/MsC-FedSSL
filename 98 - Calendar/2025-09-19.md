Look at augmentations. Strip everything as additions in the Hyperparams and compare results to a baseline that can be found in [[FedMatch]]. After this, look through [fixmatch itself](https://github.com/fbuchert/fixmatch-pytorch/blob/master/train.py), see that everything is performed correctly. After that, look at fedavg.
After that verify everything.