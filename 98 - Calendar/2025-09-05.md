What if we dont redistribute the weights to the clients? but rather we let them slowly move to them?
	"For general non-convex objectives, averaging models in
	parameter space could produce an arbitrarily bad model." By [[(A) Federated Learning Original]]
	==> This mostly is true when the models that are averaged are from different model weights initializations


Where related work?
- In the **Introduction**: mention related work only _briefly_ when you motivate why FL+SSL is underexplored (sets up your gap).
    
- In the **Background/Methodology**: cite the main FL, SSL, and FL+SSL papers in detail when you introduce FedAvg, FixMatch, etc.



***Results & Analysis:***
*Performance in different settings:*
`We tested the accuracy of the global model for different numbers of clients and for different non-iid scenarios and different lambda_u values (strength of unsupervised loss) `
We found that for our first environment where we have 10 clients, in very non-iid situations our model performs significantly worse. And the more we go to iid, we start outperforming fully labeled clients (red line). Ideally we would want that the semi-supervised aspect improves the accuracy, though we observe a negative effect of introducing pseudo-label losses.
That brings up the question, is our accuracy bad because of the SSL part of because of the averaging?
--> We perform the same tests for 1 & 2 clients to observe the effect of fedavg
For 1 client: We find that a lambda_u of 0.5 often notes a positive effect onf the accuracy (and at 1 its bad again so there is a best lambda_u to be found)
For 2 clients we see that our models suffer in non-iid environments, showing that its most likely the averaging that ruins performance.

We also wanted to see how our addition of STM improved the performance, and we see that the accuracies are slightly lower without STM in most cases in more iid-scenarios.

*We first ran test foor 100 rounds:*
We see that after pretraining ends, performance is harmed significantly especially  in lower labeled_fraction scenarios. e do see that in iid scenarios, the accuracy is able to climb again over time, which shows that in iid situations, fedavg might help over time.


Comments:
- Use multiple runs with diff random seeds with variance.
- Expect more of a gradient on lf_1
- Add lf between 0.5 - 1, understand why it doesnt work.

Discussion: what ive observed is what other's have observed, and if we found solution what solution.


Google states in their paper that 200 rounds is little but still manages to attain decent performance after 200 rounds, so we decided to do the same.

In iid we simply ouperform, introducing unsupervised loss actually decreases performance extremely much, though it does look like it might climb up again with large variance.

Next chart, per batch loss of each client:
The issue is, that the model overfits extremely hard

Important: no pretrained weights & not tuned optimally, just random hyperparams, perhaps we can improve everything.


Pretrained weights, 50 rounds, finetune hyperparameters better, Try to find if FedAvg is bottleneck
5 run variance

multiple runs to analyze variance.

Do 1 run with changing the augmentation, remove flip.

Meeting 7th of october at 10:30


Next-Steps:
- Could lower threshold 0.6 - 0.9. And learning rate can be lowered. With gridsearch this can be done. 1 lf, 1u (1 point of a grid basically) 1 alpha you see priority in (non-iid).
- Could visualize stm size per client.
Test 1: 1-2 clients, see effects of different hyperparameters. Visualize stm size at the end of round for each client.
Test 2: Pretrained weights with new hyperparameters in same scenarios as test1.
Test 3: Augmentation good or bad.
Test 4: Seeing FedAvg is bottleneck. Test 10 clients with new params with fedavg and new thing.
Test 5: 5 runs for each scenario for variance.


PLot: Seaborn Catplot might be nicer to show variance.
Discussion: Parameters can differ per scenario. If we look at non-iid then we can tune there and fix fedavg