For Fixmatch, if u have many classes u predict, u aren't rlly minimizing entropy by taking argmax.

For sure have to fixm y augmentations, [[(A) FedGGd Federated semi-supervised learning via globally guided pseudo-labeling]] has a good/simple setup i should look at, though its not baseline.

How about we make the STM ensure same size of the labels? So 30 for each or smth


They do tests with dir alpha = 0.5 in [[(A) Federated Learning on Non-IID Data Silos. An Experimental Study]]