For Fixmatch, if u have many classes u predict, u aren't rlly minimizing entropy by taking argmax. So soft max perhaps?

For sure have to fixm y augmentations, [[(A) FedGGd Federated semi-supervised learning via globally guided pseudo-labeling]] has a good/simple setup i should look at, though its not baseline.

How about we make the STM ensure same size of the labels? So 30 for each or smth & then grow over time perhaps when all are filled we grow? ==> Perhaps a solution.


What do we do about the weighting in fedavg? Do i weight by labeled dataset size or not?

They do tests with dir alpha = 0.5 in [[(A) Federated Learning on Non-IID Data Silos. An Experimental Study]]


Look at [[(A) FedMatch]] (they use original images vs weak ones for naive fixfedmatch?)


[[(A) SemiFed]] Shows that just increasing lambda_u doesnt really improve performance, its lowkey random.

Most setups do end up using 100+ - 300 rounds, 10 clients ish and then like Xk samples per class in iid. And then either dirichlet or some other way to ensure non-iid
alpha = 0.3, which is apparently standard.
Local training epochs can be 1-20 , though higher is rarely used.

for me:
Investigate my own perturbations, these might be introducing bias.


In case not equipped with these perturbations,
FixMatch will be degenerated to a naïve online self-training
pipeline, producing much worse results. from [[(A) (UNIMatch) Revisiting Weak-to-Strong Consistency  in Semi-Supervised Semantic Segmentation]]


By next time i ofc want to finish all my papers & situation, should take 2 days. and finish my introduction/background.
Go over the code and find any bugs if any exist cause i feel like my performance is lower than others'. This could be due to the fact that we mostly focus on the heavily non-iid case.
And I want to slowly add more text, such as methodology & datasets & such.

Then it'll only be testing basically & results.