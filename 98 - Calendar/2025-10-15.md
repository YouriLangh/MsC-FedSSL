typically \hat{} is being used for approximations, hence \hat{y} for DL predictions. I think it is fine here if you are conistent with the notation throughout the thesis but maybe switching to \tilde{} or simply x’ make the differentiation more clear
==> Did i do it correctly now?



Should i retain the epochs for local training (other ppl said they did that right?) or do i jsut alter my stm to be for rounds? perhaps better like this.

Struggling to know what to write where since its basically a proof of concept with minor extensions. I know i implemented it in pytorch and have made some specific assumptions and such, but how do iwrite that? Do i rewrite all teh theory to be abel to explain each change ive made in each step?

@ end of background:
Good opportunity to show your Venn diagram  :)
==> Not really as that talks about data distribution and not methods used (i put it in intro)
@ Methodology:
1) FedFex: Algo + Formalization
2) STM
==> Do you mean Main title switch to Fixfed and then section for STM?

Didnt use DDP:
	**PyTorch Distributed Data Parallel (DDP)** is designed for **centralized, synchronous distributed training**.  
	All GPUs or nodes work together to train **one single model** on a **shared dataset split** across workers.
	
> 	In contrast, **Federated Learning (FL)** is **decentralized** — each client trains **its own copy** of the model **on its own private dataset**, which **cannot be shared**.



GPU:
5:36:53 no stm done at 6:59:26 ==> 1H22M~
5:36:53 STM done at 6:59:05 ==> 1H22M~

CPU:
9:26:05 PM no stm > 03:57:32
9:26:05 PM stm >  03:54:32



**Simon:**
- Report best possible config for that model (e.g. supervised vs Fixmatch when comparing)
- Verify that 1gpu code still works

*Comments:*
> Run limited tests & try to figure out which tests are worth running.
	scale it down, 10 clients is doable.



same tests as before and also the one with stm for 5 seeds and then also a test with 5 clints setting stm on/off and more rounds 10-20 (goes in ablation)

Also one test for more clients 10.
"First we add all this and then we add stm and we see..."
If you add STM, add it in a seperate section.


And then ablation test
- for the stm: label distribution and such.


And perhaps run it for 10-20 rounds.
11 am belgian time 4/11

Check how long 1 client trains on gpu and cpu
multiprocessing library by python (native)

atleast 5 seeds.

keep 5 clients.

check how does stm improve when and such
	After this, ablation study.
Just see how many samples in stm and what we expect dependdent on adapative threshold.
How does it grow and what is label distribution and comparsion to ground truth distribution.

Adapative threshold, we use fixed as we want to be close to fixmatch
	 If we have time, how does this change and how many samples do we have in stm and how does adapative threshold adapt this

strip plots seaborn, box plot or smth else for that, make x-axis  categorical


Simon:
Small mobile devices (pacemaker) with limited storage but u want expressive model, but finding right optimum between size of model & expressiveness is important
ADd github links for algorithm