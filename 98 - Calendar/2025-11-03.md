What has to happen by tomorrow? All my tests have to finish running, And i have to run 1 centralized baseline which has high performance and is not impossiblbe to reach.

~~Prolly best to report results in a table too, this is kinda hard to understand
~~
Discussion, centralized baseline is veryyyy confusing, why is it so low?
I didnt know i had to crop+flip, is that a thing?
Without augmentation around 0.75, with its about: 

For the centralized baseline, its prolly best to either do epochsxrounds or epochsxroundsx 1+ mu or smth, just compare results

So the red line shows the possible upperbound when not using any unlabeled data, we EXPECT this to outperform all the ssl pipelines when lambda = 0 , and then we want to see if we can ever beat it by using unlabeled data



We have not performed any tests for the stm on, as this will be considered an extension

After this baseline test, we will test the 2 popular SSL extensions (STM/adaptive threshold) in a specific scenario (for all alphas) and compare it to the results we got in that scenario (for all alphas) to see if we can beat the results.

Then we will perform an ablation where we will give the results for a specific scenario again, and ablate over the datasets (we'll try 3 seeds), backbones, etc etc, nr of rounds, nr of clients, etc. Some of those will be run today already, ensuring we have all our results by december. 

What does htis prove in practice? Though not applied directly to medical data, which has possible differences in structure ofcourse to thsi data, by proving these results over a large selection of datasets and providing insights into each important component, we can shed light on the possible performance that might be overlooked.




Meeting if results are bad, just send them results otherwise
add footnote saying X isnt finetuned
Also mention that we perform these transforms on dataset to compare baslines (apples to apples)
2K for ablation 
~~fix supervised baseline~~
~~pictures for overleaf pdf~~

How do i run 20 clients, if i have to split it up??? this is kinda imposs unless i allow 10 clients and then the other 2 clients
Use `total_workers = world_size - 1` (workers with GPUs). Each worker gets a slice of client IDs: `start..end` and trains those clients sequentially on its GPU.

This makes every round 2H -> 10H total?


Ablation (backbone, clients, rounds, datasets) + extensions tests = 108 test ish + 4 for iters.
