Alter texts, iid/non-iid is altered
~~Also, we now put everything in subsets for splitiid/non-iid, and then we unwrap this, just use lists of indices to reduce overhead please.~~
~~The code on HPC is not current with the new fix for supervised training.~~
Running tests with different seeds, 20K iters and 40 labels per class now.