[[Feedback-Driven Pseudo-Label Reliability Assessment. Redefining Thresholding for Semi-Supervised Semantic Segmentation]] Does mention that the model degradation due to choice of confidence threshold does not follow a consistent pattern.
"As labeled dataset size decreases, the optimal threshold
becomes increasingly unpredictable"

I feel like this paper makes it very comlplex to get more stability out of it, isnt this basically just a class-adaptive threshold but with assessor networks to improve reliability? We could just check how many samples we have and use the confidence of each sample or smth to alter threshold pre class.

The semi-supervised is suppoed to improve performance in very-low data scenarios by a decent amount. ==> Verify we have this too (in 1 client scenarios)

FedAvg has negative effect in non-iid enviroments.

Is our non-iid distribution working for 1 client? perhaps im just high but should he just get random values of certain classes, not the same as iid


Observations:
1. We are not performing well in non-iid situations. Is this due to semi-supervised or due to FedAvg? Test 4 & 3 show that it might just be FedAvg. Meaning that our model representations might be too spread out to converge correctly. Perhaps more rounds solves this?
2. We don't always observe the upwards trend from SSL. We know that degradation of results is random due to threshold. Is ths issue with our SSL implementation or the threshold/lambda_u?
3. Results are not satisfactory, of course we haven't tuned much and no Optuna tuning.
4. We notice that STM off degrades the results slightly (Test 2 vs Test 1)

Simon:
We see that lambda_u increases performance which is above chance for labeled_rac = 0.01 and lambda_u obv degrades after that value (0.5)
Spectral normalization of weights

Improve whole model or analyse performance?
Discuss with professor.

Frovenius norm(mean & variance analysis) or MSE distance between client 
Perhaps weight regularization could help (l2 regularization perhaps on strong weights.)
weights after each round = new metric
is there one client that diverges alot

Explore all the lambda_us (not top priority)

Pick a specific labeled_fraction. Look into FedAvg, what are updates on weights (variance & distance between weights)

"Quick fix" meta learning, we have a learner, you have an additional outer loop, that tries out different weight combos each round and learns proper weight.

TODO:
- ~~Create a test with 2/3 clients which runs for longer (100 rounds) with certain fixed hyperparams (l_u = 0.5, l_f=0.2/0.1) ~~to verify if FedAvg stabilizes after a while
- ~~Add weight summary statistics (min, max, mean, average, variance and perhaps others) of both backbone & head to train logs ~~to see how weights alter over time and who could be the culprit (are box plots the best for this?)
- Create the same test as (1) but with 200 rounds, Ensure pretrain rounds = 10% of rounds
- Perform another test where we increase epochs to see how that helps.
- ~~Add the validation set  to fedavg so we can get accuracy per round for the global model~~ & see how this compares to the first 4 hpc test accuracy results.


Maybe look into weight initializations, either everyone get Xavier init. or everyone getting same one. Check if i want to use pretrained resnet18 weights (.Default)


variance of the loss for each client

(Split up 1 gpu into multiple parts and each part for 1 client 
Pytorch distributed, usually 1 wrapper)

Motivation, the intro (introduce core concepts)
Review literature

Motivation:
the gap in research we are trying to address

Methodology, the core model & the background to understand it.

Perhaps say in review that there are fields & different applications
(semantic segmentation n such, and we focus on classification) relevance in subsections of the fields.
