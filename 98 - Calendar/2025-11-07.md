What if we run 100 clients with only 5 selected?
So that would be with nr of clients to compare to fedmatch. And then we do anotehr one that is like 7 clients

Have to run some of the tests on single gpu tnite

Ran tests for 1 client and multiple local iterations.


We need anotehr test to shwo that when not using ssl and only fl, the performance is worse or smth to put the blame on smth.

# Dont forget to alter job ids if running file 2x in a row...

We are running nr of rounds 10-20

Tests to run:
Single GPU:

(ablation)
Stm on, figure out best params, perhaps use optuna for this.

Then report best results in extension results vs naive framework 

(ablation)
Adaptive thresholding, use optuna again.
Then report best results in extension results vs naive framework 

Ablation with stm & adaptive on

Different backbones (dont forget to add it to folderid)

Different datasets


Increasing local iterations

Multi-GPU
10 clients & 100 clients test(F=0.05) (clients ablation)

Test the naive FixMatch + FedAvg implementation (without Short-Term Memory or Adaptive Thresholding) as the primary configuration for backbones, number of clients, number of rounds, and datasets. This establishes a baseline performance for the core framework, addressing RQ1–RQ4.

So perhaps its even best to run tests without extensions and then rerun the base ones with each extension, tho thats not possible

It’s fine to state in your Discussion (Chapter 5) that direct comparisons to other frameworks (e.g., FedMatch, SemiFed) are limited due to computational constraints, as they use more clients and rounds, and your focus was on analyzing the naive FixMatch + FedAvg implementation with limited resources. Cite your single 10-client ablation to show partial comparability, addressing RQ1, while noting resource limitations. This is defensible and aligns with your thesis constraints.

![[2025-11-07.png]]