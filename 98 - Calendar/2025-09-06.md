Next, we will:



Next-Steps:
- Could lower threshold 0.6 - 0.9. And learning rate can be lowered. With gridsearch this can be done. 1 lf, 1u (1 point of a grid basically) 1 alpha you see priority in (non-iid).
- Could visualize stm size per client.
- Pretrained weights, 50 rounds, finetune hyperparameters better, Try to find if FedAvg is bottleneck
- 5 run variance; multiple runs to analyze variance.
- Do 1 run with changing the augmentation, remove flip.



~~Test 1: 1-2 clients, see effects of different hyperparameters. Visualize stm size at the end of round for each client.~~
Test 2: Pretrained weights with new hyperparameters in same scenarios as test1. import torchvision.models as models models.ResNet18_Weights.DEFAULT
Test 3: Augmentation good or bad.
Test 4: Seeing FedAvg is bottleneck. Test 10 clients with new params with fedavg and new thing.
Test 5: 5 runs for each scenario for variance.


PLot: Seaborn Catplot might be nicer to show variance.
Discussion: Parameters can differ per scenario. If we look at non-iid then we can tune there and fix fedavg