Supervised client uses affine still, is this the way we should do it? investigate.
Should i reuse optimizer every round?? grok says no.

Fix the dataset splitting...

Fix images of cifar10 with new augemntation if needed

alter all federated learning and semi-supervised learning with abbrev

add if-test for svhn /mnist to not flip


Need some metrics to evaluate AT (what im debugging but then perhaps save it?)

Improve flow in text

Remove certain features
add background on ML (mostly fundamentls & architectures)

Run a 250 epoch test with 4K labels (400 per class) for fixmatch to see performance and plot accuracy curve


Improve transitions.
Reframe story more, emphasize 'sensitivity analysis' of fix+fedavg which has been fallen short 

Emphasize extensions more (remove simple & naive)

We want to improve representation of these methods


We focus on ssl part, not fedavg part to avoid privacy (fedmatch, cite the 2301 paper from Zhao) 

mixed precision is implementation detail.

Metohodology: Improve on extensions.

Before we introduce FixMatch a combination of SSL and FedAvg, we first clarify the important concepts and backround on Deep Leanring

The optimizer is probably wrong (and do cosine decay perhaps)

Make a diagram of backbone-> head (architecture)

add see section when referring 
add labels to text. (wrap in figure and add label)

IssueFixed?How--n_rounds=1 → many roundsNo→ Use --n_rounds=200–500New optimizer every roundNo→ Create optimizer once in client __init__Threshold = 0.6No→ Use 0.95Low LR or wrong scheduler?→ SGD 0.03 + cosine decay