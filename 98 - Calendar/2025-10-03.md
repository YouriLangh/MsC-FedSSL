~~I will run all the fixmatch tests~~

Then all i need to do is 1. centralized test which will be our upper bound
and 
for each alpha and each labeled_amount i will need to do the supervised client tests (3 cases (all alphas and if all labeled x 3 (amount of clients))

BUT, do we want another upper bound where all clients have all the labeled data? Perhaps thats good too. So it'll be 9 tests. 
only whenn all are labeled and for each iid case and each client case
	for 5 clients stm on or off is same data.

<span style="color:rgb(255, 0, 0)">LABELED FRACTIOn less than 1 check is gone verywhere!!!! bc of bugs!!
	i added uses_all_labeled_data method (check if correct AND, implement everywhere where removed in github)
</span>
THE TESTS ARE WITH 5 ROUNDS NOT 10, NOT ALLOWED TO RUN MORE!!!


I can just run the centralized baseline and call it quits there
Discuss with simon how i can compare my supervised & fixmatch clients.
Not all my tests ran!!!
RERUN THE STM ON TESTS !!!
Optimize the code and also run supervised client tests.

use_stm code from argparse is deprecated!

job 0 of supervised got cancelled! 

For my scatterplot

|X-axis|Y-axis|Color (`hue`)|Interpretation|
|---|---|---|---|
|`lambda_u`|`val_acc_increase`|`# Labels`|Does adding more unlabeled weight (λᵤ) help?|
|`alpha`|`val_acc_increase`|`n_clients`|Does data heterogeneity affect improvement?|
|`n_clients`|`val_acc_increase`|`# Labels`|Do more clients still make progress per round?|
|`# Labels`|`val_acc_increase`|`lambda_u`|Do experiments with more labeled data improve more?|

TODO:
- Ensure if i use cpu that cpu is used and not gpu (shouldnt rlly matter if i run on cpu only cluster)
- Ensure stm works with flag (idk how i can test this)
- test cpu vs gpu diff
- report all results 
- Missing 1 result: [./hpc_results\test8_verification\aiid_labeled_total10000_lu0.5_c5_r5_e3_e2048_stmFalse_seed1555\test.json](https://file+.vscode-resource.vscode-cdn.net/c%3A/Users/neere/OneDrive/Bureaublad/All/School/MsC_Thesis/hpc_results/test8_verification/aiid_labeled_total10000_lu0.5_c5_r5_e3_e2048_stmFalse_seed1555/test.json) and all stm on results.

Simon:
Report best possible config for that model

Verify cpu & gpu check to make sure we only use gpu when i ask it

run tests on cpu

Run limited tests & try to figure out which tests are worth running.

scale it down, 10 clients is doable.

And perhaps run it for 10-20 rounds.
11 am belgian time 4/11

Check how long 1 client trains on gpu and cpu
multiprocessing library by python (native)

atleast 5 seeds.

keep 5 clients.

check how does stm improve when and such
	After this, ablation study.

Adapative threshold, we use fixed as we want to be close to fixmatch
 If we have time, how does this change and how many samples do we have in stm and how does adapative threshold adapt this
Just see how many samples in stm and what we expect dependdent on adapative threshold.
How does it grow and what is label distribution and comparsion to ground truth distribution.
strip plots seaborn, box plot or smth else for that, make x-axis  categorical

same tests as before and also the one with stm for 5 seeds and then also a test with 5 clints setting stm on/off and more rounds 10-20 (goes in ablation)

Also one test for more clients 10.
"First we add all this and then we add stm and we see..."
If you add STM, add it in a seperate section.


And then ablation test
- for the stm: label distribution and such.

